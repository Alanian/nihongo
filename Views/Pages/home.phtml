<h1>Japanisch lernen mit Nipponjaku</h1>

<h2>Āā Ēē Īī Ōō Ūū</h2>
<p>
    <?= $global->jap('日本語', 'japanese'); ?>
    <?= $global->jap('を', 'object particle', true); ?>
    <?= $global->jap('勉強', 'learning'); ?>
    <?= $global->jap('したい', 'want to do'); ?>
    <?= $global->jap('です。', 'copula; here: politeness marker'); ?>
    <br>
    <?= $global->jap('私', 'I'); ?>
    <?= $global->jap('は', 'theme particle', true); ?>
    <?= $global->jap('マンハイム', 'Mannheim, a city in Germany'); ?>
    <?= $global->jap('に', 'direction particle'); ?>
    <?= $global->jap('行きたい', 'want to go'); ?>
    <?= $global->jap('けど、', 'but; however'); ?>
    <?= $global->jap('時間', 'time'); ?>
    <?= $global->jap('が', 'subject particle'); ?>
    <?= $global->jap('ありません。', 'aren\'t; haven\'t'); ?>
</p>

<h3>Math Operations</h3>
<p>
    You can do basic math operations to numerical values and colors. Lets say we want to have two divs placed next to
    each other, the second one being twice as wide and with a different background. LESS knows what the measuring units
    are and won't mess them up. You can do basic math operations to numerical values and colors. Lets say we want to
    have two divs placed next to each other, the second one being twice as wide and with a different background. LESS
    knows what the measuring units are and won't mess them up.
</p>